# smart-prompt-optimizer
SmartPrompt Optimizer is a Python-based tool that helps users improve, evaluate, and refine prompts for Large Language Models (LLMs). It takes a user’s raw prompt and transforms it into a structured, high-quality prompt using prompt engineering techniques like:  Role assignment,Instruction,clarity Output,formatting,Constraint,addition
